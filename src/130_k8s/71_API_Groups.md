API:
* core ```/api``` 
* named ```/apis```
  * ```/apps```
    * ```/v1/deployments```
    * ```/v1/replicasets```
    * ```/v1/statefulsets```
  * ```/extensions```
  * ```/networking.k8s.io```
  * ```/storage.k8s.io```
  * ```/authentication.k8s.io```
  * ```/certificates.k8s.io```