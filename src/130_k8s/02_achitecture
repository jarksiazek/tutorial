* Master Node
  * kube-apiserve
  * ETCD Cluster
  * kube controller
   * check status of the nodes
   * remediate situtation
   * monitor the situation of the components 
  * kube scheduler
   * selects proper node for pod 
 * Nodes
  * Kublet
   * load and unload containers to node
   * monitor contrainers in nodes
  * Kube proxy
   * runs on each node in kubernetes cluster
   * helps to comunicate between pods e.g database to webapp
  
   

  
